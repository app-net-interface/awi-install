apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "awi-operator.fullname" . }}-controller-manager
  labels:
  {{- include "awi-operator.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.controllerManager.serviceAccount.annotations | nindent 4 }}